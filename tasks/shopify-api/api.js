// import request from './request';
// import {material} from './data/meta_products';

// POST the global store metafield data for the products material
// request(post, '/admin/metafields.json', material);
// request(config(), 'post', '/admin/metafields.json', material);

// request('GET', 'admin/metafields.json');

// config().get('/admin/metafields.json', (err, data, headers) => {
//     console.log(data); // Data contains product json information
//     console.log(headers); // Headers returned from request
// });
