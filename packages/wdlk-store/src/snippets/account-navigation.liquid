<div class="AccountNavigation">
    {% comment %}
      If customer accounts are enabled, provide login and create account links
    {% endcomment %}
    {% if shop.customer_accounts_enabled %}
    <div class="AccountNavigation-list">
    {% if customer %}
        {% if customer.first_name != blank %}
        {% capture first_name %}<a href="/account" class="AccountNavigation-item">{{ customer.first_name }}</a>{% endcapture %}
        {{ 'layout.customer.logged_in_as_html' | t: first_name: first_name }}
        {% else %}
        <a href="/account" class="AccountNavigation-item">{{ 'layout.customer.account' | t }}</a>
        {% endif %}
        <a href="/account/logout" class="AccountNavigation-item">{{ 'layout.customer.log_out' | t }}</a>
    {% else %}
        <a href="/account/login" class="AccountNavigation-item">{{ 'layout.customer.log_in' | t }}</a>
        {% if shop.customer_accounts_optional %}
        <a href="/account/register" class="AccountNavigation-item">{{ 'layout.customer.create_account' | t }}</a>
        {% endif %}
    {% endif %}
    </div>
    {% endif %}

    {% include 'shopping-bag' with 'link' %}
</div>
