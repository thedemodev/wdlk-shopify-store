<main class="Container Container--customer">
    <aside class="Container-sidebar">
        {% assign linkListHandle = 'footer' %}
        {% include 'link-list' with 'primary' %}
    </aside>
    <section class="Container-section TextBlock">
        <h1 class="Headline Headline--1">{{ page.title }}</h1>
        {{ page.content }}
    </section>
</main>
